<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/logoo.png">
    <title>마이페이지 - 세명패스</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <!-- 네비게이션 바 -->
    <header class="header">
        <nav class="nav">
            <a href="/" class="logo"><img src="/logoo.png" alt="logo"></a>
            <ul class="nav-menu">
                <li><a href="/board?tag=공모전">공모전</a></li>
                <li><a href="/board?tag=진로">진로</a></li>
                <li><a href="/board">보고서</a></li>
                <li><a href="/board/write">글쓰기</a></li>
            </ul>
            <div class="user-menu">
                <span><%= user.nickname %>님</span>
                <a href="/user/mypage">마이페이지</a>
                <a href="/auth/logout" class="logout-btn">로그아웃</a>
            </div>
        </nav>
    </header>

    <!-- 마이페이지 -->
    <div class="mypage">
        <!-- 프로필 섹션 -->
        <div class="profile-section">
            <div style="display: flex; align-items: center; gap: 2rem;">
                <div class="profile-avatar">
                    <%= userInfo.nickname.charAt(0) %>
                </div>
                <div>
                    <h2 style="margin-bottom: 0.5rem;"><%= userInfo.nickname %></h2>
                    <% if (userInfo.email) { %>
                        <p style="color: var(--text-secondary); margin-bottom: 0.25rem;"><%= userInfo.email %></p>
                    <% } %>
                    <% if (userInfo.school && userInfo.grade) { %>
                        <p style="color: var(--text-secondary);"><%= userInfo.school %> <%= userInfo.grade %>학년</p>
                    <% } %>
                </div>
            </div>
            <% if (userInfo.bio) { %>
                <p style="margin-top: 1.5rem; color: var(--text-secondary); padding-top: 1.5rem; border-top: 1px solid var(--border);">
                    <%= userInfo.bio %>
                </p>
            <% } %>
        </div>

        <!-- 내가 쓴 글 -->
        <div class="my-posts-section">
            <h3 style="margin-bottom: 1.5rem; font-size: 1.5rem;">내가 쓴 글 (<%= myPosts.length %>개)</h3>
            
            <% if (myPosts && myPosts.length > 0) { %>
                <% myPosts.forEach(function(post) { %>
                    <div class="post-item">
                        <div style="flex: 1; cursor: pointer;" onclick="location.href='/board/view/<%= post.id %>'">
                            <h4 style="margin-bottom: 0.5rem; font-size: 1.125rem;"><%= post.title %></h4>
                            <div style="font-size: 0.875rem; color: var(--text-secondary);">
                                <span><%= new Date(post.created_at).toLocaleDateString('ko-KR') %></span>
                                <span style="margin-left: 1rem;">조회 <%= post.views || 0 %></span>
                            </div>
                            <% if (post.tags) { %>
                                <div class="post-tags" style="margin-top: 0.75rem;">
                                    <% post.tags.split(',').forEach(function(tag) { %>
                                        <span class="tag"><%= tag %></span>
                                    <% }); %>
                                </div>
                            <% } %>
                        </div>
                        <div class="post-actions">
                            <button class="edit-btn" onclick="location.href='/board/edit/<%= post.id %>'">수정</button>
                            <form action="/board/delete/<%= post.id %>" method="POST" style="display: inline;" onsubmit="return confirm('정말 삭제하시겠습니까?');">
                                <button type="submit" class="delete-btn">삭제</button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div style="text-align: center; padding: 3rem; color: var(--text-secondary);">
                    <p style="font-size: 1.125rem; margin-bottom: 1rem;">아직 작성한 글이 없습니다.</p>
                    <a href="/board/write" class="cta-btn">첫 글 작성하기</a>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>